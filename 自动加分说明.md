# 每天半自动加分（每人 +1）

## 已准备好的文件
- `semi_auto_score.py`：主脚本（读取打卡名单并自动网页加分）
- `score_config.json`：网站地址和页面选择器配置
- `run_daily_score.command`：双击运行

## 第一次使用（只做一次）
1. 安装依赖：
   - `pip3 install playwright`
   - `python3 -m playwright install chromium`
2. 双击 `run_daily_score.command`
3. 浏览器会打开，你先手动登录并进入“班级宠物积分”页面
4. 回到终端按 Enter，脚本开始逐个学生加分

## 每天使用
1. 先把当天打卡数据写入 `attendance/YYYY-MM-DD.json`（你现在这套流程已经有了）
2. 双击 `run_daily_score.command`
3. 登录后按 Enter，等待完成
4. 结果日志在 `score_logs/YYYY-MM-DD.json`

## 常见需要改的地方
如果脚本报“找不到搜索框/加分按钮”，改 `score_config.json` 里的 `selectors`：
- `search_input`：搜索框选择器
- `student_card`：学生卡片选择器
- `add_button`：加分按钮选择器

示例（按你当前页面可再微调）：
```json
{
  "search_input": "input[placeholder*='搜索']",
  "student_card": "div",
  "add_button": "button:has-text('加分')"
}
```

